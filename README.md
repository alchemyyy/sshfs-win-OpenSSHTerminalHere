# sshfs-win-OpenSSHTerminalHere
Adds an item to the folder and background context menus of sshfs-win network drive items to open an ssh terminal session to the corresponding server at the corresponding directory
